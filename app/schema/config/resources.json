{
    "_": "DO NOT MODIFY: FILE GENERATED AUTOMATICALLY BY HOLACLIENT",
    "name": "config.resources",
    "properties": {
        "memory": {
            "type": "number",
            "pointer": "resources.memory",
            "default": 2048,
            "minLength": 1,
            "maxLength": 20,
            "pattern": "^[0-9]+$",
            "description": "Default memory limit for new users.",
            "required": true
        },
        "cpu": {
            "type": "number",
            "pointer": "resources.cpu",
            "default": 100,
            "minLength": 1,
            "maxLength": 20,
            "pattern": "^[0-9]+$",
            "description": "Default CPU limit in % for new users.",
            "required": true
        },
        "disk": {
            "type": "number",
            "pointer": "resources.disk",
            "default": 3072,
            "minLength": 1,
            "maxLength": 20,
            "pattern": "^[0-9]+$",
            "description": "Default disk limit for new users.",
            "required": true
        },
        "allocations": {
            "type": "number",
            "pointer": "resources.allocations",
            "default": 1,
            "minLength": 1,
            "maxLength": 20,
            "pattern": "^[0-9]+$",
            "description": "Default allocations limit for new users.",
            "required": true
        },
        "backups": {
            "type": "number",
            "pointer": "resources.backups",
            "default": 1,
            "minLength": 1,
            "maxLength": 20,
            "pattern": "^[0-9]+$",
            "description": "Default backups limit for new users.",
            "required": true
        },
        "databases": {
            "type": "number",
            "pointer": "resources.databases",
            "default": 1,
            "minLength": 1,
            "maxLength": 20,
            "pattern": "^[0-9]+$",
            "description": "Default databases limit for new users.",
            "required": true
        },
        "subdomains": {
            "type": "number",
            "pointer": "resources.subdomains",
            "default": 1,
            "minLength": 1,
            "maxLength": 20,
            "pattern": "^[0-9]+$",
            "description": "Default subdomains limit for new users.",
            "required": true
        }
    }
}