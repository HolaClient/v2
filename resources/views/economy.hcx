meta = ({
    id: "index", async: false, layout: "client",
    document: { title: "Economy", description: hc.settings.branding.seo.description },
    permissions: { level: 0, intent: "home" }
});

data = (req) => (
    <>
        <div class="w-full px-4 z-0">
            
        </div>
    </>
)

module.exports = { meta, data };